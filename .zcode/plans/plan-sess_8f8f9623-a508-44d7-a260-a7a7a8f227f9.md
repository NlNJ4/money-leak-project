# Finish: end-to-end LINE webhook verification (2 minutes)

Everything is already prepared — service_role key moved into the gitignored `.env.local`, `.env.example` restored to placeholders (the secret was never committed or pushed), and a test LINE identity (`Utest123` → test user) is seeded in the database. The dev server on port 3000 is running with the new env.

## Remaining steps
1. **Send the signed test webhook** — a `กินข้าว 120` LINE message event with a valid HMAC signature to `http://localhost:3000/api/line/webhook`. Expect `200 {"ok":true}`; Gemini 3.7 Flash may be overloaded, in which case the 2.5 fallback parses it.
2. **Verify the database row** — `transactions` should now contain `expense / 120 / food / source=line` for the test user.
3. **Clean up** — delete the test user (cascades: identity + test transaction), leaving the DB exactly as production state.
4. **Git check** — confirm no tracked files changed (the secret fix touched only gitignored/placeholder files); nothing to commit if clean.

Then report the result. No code changes are expected in this pass.